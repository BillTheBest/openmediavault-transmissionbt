# This goes into /etc/rsyslog.d/

# All log-entries from transmission-daemon are going into /var/log/transmissionbt.log
:programname, isequal, "transmission-daemon" /var/log/transmissionbt.log

# the line below is for the log output ONLY in /var/log/transmissionbt.log
& ~
